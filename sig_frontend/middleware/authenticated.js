export default function ({ store, redirect }) {
  // If the user is not authenticated
 /*  if (store.state.profile.authenticated && store.state.maps.currentMap.privacy.toLowerCase() === "PRIVATE".toLowerCase()) {
    return this.$router.push('/auth')
  } */
}